import styled, {css} from 'styled-components/native';
import Board from 'react-native-dnd-board';

export const CBoard = styled(Board)`
  background-color: #fff;
`;

export const Wrapper = styled.View``;
